spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev
    username: devdml
    password: 1qaz@WSX
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true  # SQL ???
    show-sql: true  # SQL ??

# TODO ? ??? ?? ????! ??? ??? ? ? ?????!
# axon.autoconfig.exclude=EventHandling, EventStore, CommandBus, QueryBus

logging.level:
  org.hibernate.type: TRACE  # SQL ????? ?? : Spring Boot 3.0.0 ??
  org.hibernate.orm.jdbc: TRACE  # SQL ????? ?? : Spring Boot 3.0.0 ??
